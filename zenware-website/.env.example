# Database
# PostgreSQL connection string (supports Neon, Supabase, local PostgreSQL)
DATABASE_URL="postgresql://username:password@localhost:5432/zenware?schema=public"

# Next.js / NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret-key-generate-with-openssl-rand-base64-32

# AWS SES Configuration for Email Sending (optional)
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=us-west-2
FROM_EMAIL=noreply@yourdomain.com

# Anthropic API Key (for AI Strategy generation)
ANTHROPIC_API_KEY=sk-ant-api03-your-anthropic-api-key

# Admin Credentials (hardcoded in auth.ts for demo - change in production)
# Default: admin@zenware.ai / zenware123
